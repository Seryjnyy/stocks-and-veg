import { atom } from "jotai";

// TODO feel like when using this there should be a provider, also this does not need to be global
export const currentSectionInGroupPageAtom = atom("");

export const isWorkEnabledAtom = atom(true);
